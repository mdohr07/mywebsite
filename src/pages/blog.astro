---
import Layout from "../layouts/Layout.astro";
import { getCollection } from "astro:content";

const posts = await getCollection("blog", ({ data }) => !data.draft);
---

<Layout>
  <div class="blog-container">
    <h1>Blog</h1>

    <ul>
      {
        posts.map((post) => (
          <li class="post-item">
            <div class="post-header">
              {/* <span class="post-date">{post.data.pubDate}</span> */}
              <a href={`/posts/${post.slug}`}>{post.data.title}</a>
            </div>
            <p class="post-description">{post.data.description}</p>
          </li>
        ))
      }
    </ul>
  </div>
</Layout>

<!-- ---
import Layout from "../layouts/Layout.astro";

const modules = import.meta.glob("../pages/posts/*.md", { eager: true });
const allPosts = Object.values(modules);

const posts = allPosts.sort((a: any, b: any) => {
  return new Date(b.frontmatter.pubDate).getTime() - new Date(a.frontmatter.pubDate).getTime();
});

const formatDate = (pubDate: any) => {
  return new Date(pubDate).toLocaleDateString("de-DE", {
    day: "numeric", // 1
    month: "long", // July
    year: "numeric", // 2022
  });
};
---

<Layout>
  <div class="blog-container">
    <h1 style="margin-top: 2rem;">Mein Blog</h1>

    <p>
      Ein Logbuch des Alltags: kleine Ausschnitte, ein wenig von allem. Ich habe hier eine <a href="/tags">Liste von Tags</a> aus meinen Blog-Posts
      und <a href="https://mdohr.bearblog.dev/blog/" target="_blank" rel="noopener">ältere Beiträge</a> auf Englisch bei Bear. Ich schreibe hier
      auf Deutsch – vorerst als Experiment.
    </p>

    <hr>

    <ul class="post-list">
      {
        posts.map((post: any) => (
          <li class="post-item">
            <div class="post-header">
              <a href={post.url} class="post-title">
                {post.frontmatter.title}
              </a>
              <span class="post-date">{formatDate(post.frontmatter.pubDate)}</span>
            </div>
            <p class="post-description">{post.frontmatter.description}</p>
          </li>
        ))
      }
    </ul>
  </div>
</Layout> -->
