---
import fs from 'node:fs/promises';
import { marked } from 'marked';

const markdown = await fs.readFile(new URL('../pages/log.md', import.meta.url), 'utf-8');
const html = marked.parse(markdown);
---

<article class="prose" set:html={html} />
